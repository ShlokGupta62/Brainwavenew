{
  "name": "logihub-contact-page",
  "version": "1.0.0",
  "description": "LogiHub Contact Page",
  "scripts": {
    "build": "mkdir -p public && cp *.html *.css *.js *.json public/ 2>/dev/null || true"
  },
  "devDependencies": {
    "@vercel/static-build": "^1.3.1"
  }
}